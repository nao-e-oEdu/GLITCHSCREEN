{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",

  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run vite\"",
    "server": "npx json-server --watch db.json --port 5000",
    "test": "echo \"Error: no test specified\" && exit 1",
    "vite": "npm run dev --prefix ../vite-project"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "react-router-dom": "^7.5.2",
    "sqlite3": "^5.1.7"
  },
  
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
